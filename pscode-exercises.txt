// Exercise 1: Calculate the Sum of Two Numbers

// Input:
//   num1: First number
//   num2: Second number

// Output:
//   sum: Sum of the two numbers

// Read the input numbers
num1 = ReadNumber()
num2 = ReadNumber()

// Calculate the sum
sum = num1 + num2

// Output the result
Output(sum)
__________________________________________________________________________________

// Exercise 2: Check if a Number is Even or Odd

// Input:
//   number: The number to be checked

// Check if the number is divisible by 2 (even)
if number % 2 == 0 then
    Output("The number is even")
// Otherwise, it is odd
else
    Output("The number is odd")
____________________________________________________________________________________

// Exercise 3: 
Write pseudo code that will perform the following.    

a) Read in 5 separate numbers.   
b) Calculate the average of the five numbers.
c) Find the smallest (minimum) and largest (maximum) of the five entered numbers.
d) Write out the results found from steps b and c with a message describing what they are

a) 
// Define an array to store the numbers
numbers = array of size 5

// Loop to read in 5 numbers
for i = 0 to 4 do
    // Read a number and store it in the array
    numbers[i] = ReadNumber()
end for

b)
// Define an array to store the numbers
numbers = array of size 5

// Loop to read in 5 numbers
for i = 0 to 4 do
    // Read a number and store it in the array
    numbers[i] = ReadNumber()
end for

// Calculate the sum of the numbers
sum = 0
for i = 0 to 4 do
    sum = sum + numbers[i]
end for

// Calculate the average
average = sum / 5

// Output the average
Output("The average is: " + average)

c)
// Define an array to store the numbers
numbers = array of size 5

// Loop to read in 5 numbers
for i = 0 to 4 do
    // Read a number and store it in the array
    numbers[i] = ReadNumber()
end for

// Assume the first number is both the minimum and maximum
minimum = numbers[0]
maximum = numbers[0]

// Find the minimum and maximum
for i = 1 to 4 do
    if numbers[i] < minimum then
        minimum = numbers[i]
    end if
    
    if numbers[i] > maximum then
        maximum = numbers[i]
    end if
end for

// Output the minimum and maximum
Output("The minimum number is: " + minimum)
Output("The maximum number is: " + maximum)

d)
// Assuming the numbers entered were 10, 5, 8, 12, and 3

// Results for calculating the average of the five numbers
The average is: 7.6

// Results for finding the smallest and largest of the five entered numbers
The minimum number is: 3
The maximum number is: 12

-- END --
